---
const { 
  color = "text-100", 
  thickness = "1px", 
  margin = "my-4",
  margin_sides="6",
  width = "w-full"
} = Astro.props;
---

<hr class={`${margin} ${width} mr-${margin_sides} ml-${margin_sides} border-t-[${thickness}] border-${color}`} />
